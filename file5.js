// Declaring a constant :
// Constants are not variables as their value cannot be changed once declared.

const pi = 3.1415;
console.log(pi);
console.log(pi * 2 * 2);
console.log(pi / 3.1415);
console.log(typeof pi);

const r = 1;

const circumference = (2 * pi * r);
console.log(circumference);